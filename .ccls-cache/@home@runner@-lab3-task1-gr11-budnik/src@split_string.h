/**
Created by Kirill Budnik
**/
#ifndef SPLIT_STRING_H
#define SPLIT_STRING_H
/**
void split_string(const char *input, char *evenIndexes, char *oddIndexes) - splits string into two strings, one with even indexes and one with odd indexes
**/
#include <stdio.h>
#include <string.h>

void split_string(const char *input, char *evenIndexes, char *oddIndexes);

#endif